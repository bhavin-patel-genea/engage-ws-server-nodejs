Schema Readme:
==============

This folder contains JSON schema definitions used by the Engage Websocket Server POC.

Specifically, these schema define the required JSON elements needed by incoming and outgoing JSON data -- validating that the required JSON data is present according to any interface expectations.
